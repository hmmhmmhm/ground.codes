{
  "name": "ground.codes",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "production:smoke": "node scripts/production-smoke.mjs",
    "data:audit-labels": "pnpm --filter ground-codes test:data-audit",
    "data:report-labels": "node scripts/url-label-report.mjs",
    "runtime:check-pins": "node scripts/check-api-runtime-pins.mjs",
    "runtime:update-pins": "node scripts/update-api-runtime-pins.mjs",
    "scripts:test": "node --test scripts/*.test.mjs",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.{ts,tsx,md}": [
      "pnpm --filter web lint",
      "pnpm --filter web check-types",
      "pnpm --filter web format"
    ]
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "15.5.0",
    "prettier": "^3.5.3",
    "turbo": "^2.9.12",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
